import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.speakeng.app',
  appName: 'English Speaking Partner',
  webDir: 'dist'
};

export default config;
